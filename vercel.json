{
    "version": 2,
    "buildCommand": "flask run",
    "installCommand": "pip install -r requirements.txt",
    "builds": [
        {
            "src": "./app.py",
            "use": "@vercel/python"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/"
        }
    ]
}